name: .NET

on:
  push:
    branches: [ "dev","workflows" ]
  pull_request:
    branches: [ "dev" ]

jobs:
  build:

    runs-on: ubuntu-latest
    environment: TEST

    steps:
      - name: Script
        run: |
          echo "Hello World!"
          echo "${{ secrets.EZORED_AWS_KEY_ID }}"
