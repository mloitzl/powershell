name: .NET

on:
  push:
    branches: [ "dev","workflows" ]
  pull_request:
    branches: [ "dev" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Script
        run: |
          echo "Hello World!"
